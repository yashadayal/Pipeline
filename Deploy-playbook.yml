---
- name: Deploy Docker Image
  hosts: all
  become: true
  tasks:
    - name: Pull Project Image from Docker Hub
      docker_image:
        name: docker.io/yasha145/dockerproject_1
        source: pull
