---
- name: Deploy Docker Image
  hosts: all
  become: true
  vars:
    docker_image: "docker.io/yasha145/dockerproject_1"
  tasks:
    - name: Pull Project Image from Docker Hub
      docker_image:
        name: "{{ docker_image }}"
        source: pull
