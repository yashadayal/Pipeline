---
- name: Deploy Docker Image
  hosts: all
  tasks:
    - name: Docker service
      shell: sudo service docker start
       
    - name: Pull Project Image from Docker Hub
      shell: docker pull yasha145/dockerproject_1:latest 
      
      
